[project]
name = "coral-gh-scripts"
version = "0.1.0"
requires-python = "==3.13.*"
description = "GitHub Actions helper scripts for coral"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
