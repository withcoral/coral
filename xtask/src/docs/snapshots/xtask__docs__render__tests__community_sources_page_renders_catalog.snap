---
source: xtask/src/docs/render.rs
expression: "community_sources_page(&[demo, minimal])"
---
---
title: "Community sources"
description: "Discover community-maintained Coral source specs and import them into your workspace."
---

{/* AUTO-GENERATED — DO NOT EDIT. Run `make docs-generate` to update. */}

Community sources are SourceSpec files built and maintained by our community. This page lists active community entries that use `spec_version: 1`, `kind: source`.

<Note>
  `coral source discover` lists only active bundled SourceSpec entries compiled into the CLI.
  Import community sources with `coral source add --file`.
</Note>

## Install a community source

Review the source's `SourceSpec` YAML, then add it from its file path:

```shellscript
coral source add --file path/to/source.yaml
coral source test <name>
```

If a source declares inputs, Coral reads them from environment variables by default. Add `--interactive` to be prompted instead:

```shellscript
coral source add --interactive --file path/to/source.yaml
```

After importing, the source behaves like any other installed source. It appears in `coral source list`, works with `coral source info <name> --verbose`, and exposes generated exports through MCP `search`/`describe`. SQL projections are available through the CLI and Code Mode when the generated source exports include SQL bindings.

## Available community sources

| Source | Description |
| --- | --- |
| [demo](https://github.com/withcoral/coral/tree/main/sources/community/demo) | A small demo source used in snapshot tests |
| [minimal](https://github.com/withcoral/coral/tree/main/sources/community/minimal) | Coral community source: minimal |

## If a source is missing

If the source you need does not have an active bundled or community SourceSpec yet, [write a custom source spec](/guides/write-a-custom-source). If the source is generally useful, open a pull request that adds the SourceSpec YAML and source-specific README.
