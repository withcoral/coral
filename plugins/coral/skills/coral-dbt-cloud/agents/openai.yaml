interface:
  display_name: "Coral Dbt Cloud"
  short_description: "Query dbt Cloud jobs, runs, and environments"
  default_prompt: "Use $coral-dbt-cloud to query dbt Cloud job health, run failures, and environment status."
