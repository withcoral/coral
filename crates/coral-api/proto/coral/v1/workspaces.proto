syntax = "proto3";

package coral.v1;

import "coral/v1/resources.proto";

// A user's role within one workspace.
enum WorkspaceRole {
  // The role is unknown or was not provided.
  WORKSPACE_ROLE_UNSPECIFIED = 0;
  // May read and manage the workspace.
  WORKSPACE_ROLE_OWNER = 1;
  // May read the workspace.
  WORKSPACE_ROLE_MEMBER = 2;
}

// One user who belongs to a workspace.
message WorkspaceMember {
  // Stable internal user identifier.
  string user_id = 1;
  // User's role within the workspace.
  WorkspaceRole role = 2;
  // Configured display name, or empty when unavailable.
  string display_name = 3;
}

// One workspace visible to the current user and their role within it.
message WorkspaceMembership {
  // Visible workspace resource.
  Workspace workspace = 1;
  // Current user's role within the workspace.
  WorkspaceRole role = 2;
}

// Lists workspaces visible to the current user.
message ListWorkspacesRequest {}

// Returns the current user's workspace memberships.
message ListWorkspacesResponse {
  // Visible workspaces and the current user's role in each.
  repeated WorkspaceMembership memberships = 1;
}

// Creates one workspace owned by the current user.
message CreateWorkspaceRequest {
  // Workspace resource to create.
  Workspace workspace = 1;
}

// Returns the created workspace.
message CreateWorkspaceResponse {
  // Created workspace resource.
  Workspace workspace = 1;
}

// Deletes one workspace and its managed artifacts.
message DeleteWorkspaceRequest {
  // Workspace resource to delete.
  Workspace workspace = 1;
}

// Returns the deleted workspace.
message DeleteWorkspaceResponse {
  // Deleted workspace resource.
  Workspace workspace = 1;
}

// Lists members of one workspace.
message ListWorkspaceMembersRequest {
  // Workspace whose members should be listed.
  Workspace workspace = 1;
}

// Returns members of one workspace.
message ListWorkspaceMembersResponse {
  // Workspace members.
  repeated WorkspaceMember members = 1;
}

// Adds one user to a workspace.
message AddWorkspaceMemberRequest {
  // Workspace to modify.
  Workspace workspace = 1;
  // User and role to add. The display name is ignored.
  WorkspaceMember member = 2;
}

// Returns the added or existing identical membership.
message AddWorkspaceMemberResponse {
  // Added workspace member.
  WorkspaceMember member = 1;
}

// Removes one user from a workspace.
message RemoveWorkspaceMemberRequest {
  // Workspace to modify.
  Workspace workspace = 1;
  // Stable internal identifier of the user to remove.
  string user_id = 2;
}

// Confirms removal of one workspace member.
message RemoveWorkspaceMemberResponse {}

// Workspace management APIs.
service WorkspaceService {
  // Lists workspaces visible to the current user.
  rpc ListWorkspaces(ListWorkspacesRequest) returns (ListWorkspacesResponse);
  // Creates one workspace owned by the current user.
  rpc CreateWorkspace(CreateWorkspaceRequest) returns (CreateWorkspaceResponse);
  // Deletes one workspace and its managed artifacts.
  rpc DeleteWorkspace(DeleteWorkspaceRequest) returns (DeleteWorkspaceResponse);
  // Lists members of one workspace.
  rpc ListWorkspaceMembers(ListWorkspaceMembersRequest) returns (ListWorkspaceMembersResponse);
  // Adds one user to a workspace.
  rpc AddWorkspaceMember(AddWorkspaceMemberRequest) returns (AddWorkspaceMemberResponse);
  // Removes one user from a workspace.
  rpc RemoveWorkspaceMember(RemoveWorkspaceMemberRequest) returns (RemoveWorkspaceMemberResponse);
}
