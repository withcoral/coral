# Schema v3 artifact fixtures

These files freeze the current schema-v3 YAML contract. They guard direct
deserialization against accidental model drift; they do not claim support for
migrating from an older artifact schema.
